(library
 ((name incr_map_benchmarks)
  (flags (:standard -safe-string))
  (libraries (
    core
    incremental_kernel
    incr_map
    ))
  (preprocess (pps (ppx_jane ppxlib.runner)))
  ))


(jbuild_version 1)
