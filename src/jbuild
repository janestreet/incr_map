(library
 ((name incr_map)
  (public_name incr_map)
  
  (flags (:standard -safe-string))
  (libraries (core_kernel incremental_kernel))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))


(jbuild_version 1)
